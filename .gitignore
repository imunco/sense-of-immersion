# 仓库根目录的临时脚本 —— 绝不要让它们进仓库（曾经把一个带 PAT 的 1.ps1 提交进来过）
/*.ps1
/*.sh
/*.cmd
/*.bat
/*.log

# 绝不要提交任何本地口令文件
.dsh-passphrase.local
*.passphrase
.env

# 离线备份里有明文，绝不提交
backups/
.dsh-watchdog.log

.vercel/
# Vercel 的构建产物
/public/

node_modules/
tests/shots/

# 预告片的中间产物与成片：可以重跑，别进仓库（见 trailer/README.md）
trailer/stills/
trailer/out/
package-lock.json
.DS_Store
Thumbs.db

.vercel
.env*