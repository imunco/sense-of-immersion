# SECURITY.md · 一线千愿的防护与边界

先说最重要的一句：**这个站没有服务端。** 前台是 GitHub Pages 上的静态文件，
写入经过一个公共中转站（ntfy.sh），归档落在公开仓库里。
"后台安全"在这里必须是另一套思路——不是"挡住访问"，而是"让敏感数据即使被拿到也读不懂"。

---

## 一、先分清哪些是公开的，哪些要保护

**用户在提交时必须选一次：公开，还是仅自己可见。** 这是这套安全模型的起点。

| 数据 | 公开愿望 | 仅自己可见的愿望 |
|---|---|---|
| 署名（name） | 公开 | **加密**（只有后台口令能解开） |
| 愿望正文（wish） | 公开 | **加密**（同上） |
| 写下时间、丝线颜色 | 公开 | 加密 |
| 语言 / 时区 / 设备 / 视口 / 来源页 / 设备标识 | **加密** | **加密** |

三句话说完：

1. **选择「公开」的愿望，署名和正文就是公开的**——蛛网上每颗露珠都能被读到，这是产品设定，口令不保护它，也不该保护它。
2. **选择「仅自己可见」的愿望，正文在中转站和仓库里都只有密文。** 它不会出现在蛛网上，也永远不会出现在公开归档文件里。只有持有后台口令的人能解开。
3. **无论选哪个，语言 / 时区 / 设备 / 来源这些基础信息一律加密。**

---

## 二、敏感数据的三层保护

```
浏览器                       中转站(ntfy)          采集器(Action)              仓库
──────────────               ────────────          ─────────────             ─────────
公开：name/wish ────────────► 明文 ──────────────► 审查 ────────────────────► data/wishes.jsonl（公开）
私密：ECIES(name/wish) ─────► 密文 ──────────────► 拆开审查 ── DEK 再加密 ──► data/private/vault.jsonl（密文）
基础信息：ECIES(deviceInfo) ► 密文 ──────────────► 拆开 ────── DEK 再加密 ──► data/private/meta.jsonl（密文）
命中规则的内容 ─────────────► 密文 ──────────────► 拆开审查 ── DEK 再加密 ──► data/queue.jsonl（整条密文）
```

仓库里没有任何一份明文的基础信息，也没有任何一份明文的私密愿望。
（采集器在内存里会短暂持有明文——它必须审查内容。这一点无法省略，见文末。）

1. **传输中（浏览器 → 中转站）**：ECIES。临时 ECDH(P-256) → HKDF-SHA256 → AES-256-GCM。
   站点公钥公开在 `data/config.json`，私钥用口令派生密钥加密后放在 `data/private/keys.json`。
   谁盯着 ntfy 话题，也只能看到 372 字节的密文。
2. **落盘（仓库里）**：AES-256-GCM。密钥用的是随机生成的**数据密钥（DEK）**，
   而 DEK 被 PBKDF2-SHA256(口令, 盐, 25 万次) 派生出的 **KEK** 包在 `data/private/keys.json` 里。
   分两层的好处：**换口令只需要重新包一次 DEK，历史元数据和私密愿望全部照旧可读。**
3. **打开（后台）**：口令在你自己的浏览器里派生密钥，只有解锁成功才会去解密。
   密钥只存在内存，刷新即失效。

### 口令本身

- **不落盘、不上传、不进 localStorage。** 仓库里只有盐、迭代次数和一段加密校验块。
- 校验方式是"能不能解开 `data/private/verifier.json`"，不是比对哈希——
  所以离线爆破要真的跑 25 万次 PBKDF2 每猜一次，而不是比对字符串。
- 连续错 5 次锁定 60 秒（本机）。
- 闲置 30 分钟自动锁定并清空内存里的密钥。

实测：解锁约 0.5 秒（一次 PBKDF2），错误口令拒绝，密文被篡改会被 AES-GCM 直接检出。

---

## 三、写入侧：防止被乱用

采集器是唯一能把内容写进公开归档的角色，所有规则都在 `scripts/collect.mjs` 里执行，
配置在 `data/moderation.json`（改完提交即生效，不用重新部署）。

| 防线 | 做法 | 实测结果 |
|---|---|---|
| 形状校验 | id 必须匹配 `w_[a-z0-9_]{4,40}`；署名 ≤24 字；愿望 2–160 字；丝线白名单；时间戳不许穿越 | 超长署名 → 丢弃 |
| 工作量证明 | 提交前必须在浏览器里挖出 nonce，使 `sha256(id+"|"+nonce)` 前 4 位为 0 | 不带证明 → 丢弃 |
| 蜜罐 | 隐藏输入框被填 → 丢弃 | 填了 → 静默丢弃 |
| 填写时长 | < 2 秒完成 → 判为机器人 | 前端会自动等够 2.4 秒，真人无感 |
| 内容审查 | 违禁词、链接、重复字符、纯符号 → **隔离待审，不删除** | 5 类载荷全部拦下 |
| 每设备限流 | 同一设备 1 小时最多 6 条 | 第 7、8 条进隔离 |
| 匿名桶 | 没有可解密设备标识的，1 小时最多 20 条（更紧） | 防止伪造设备号绕过 |
| 内容去重 | 相同署名+愿望 24 小时内只收 1 条 | 重复提交被丢弃 |
| 全站配额 | **不依赖任何客户端字段**，一小时内最多收 240 条，超出全部进隔离 | 伪造设备号刷屏也撞这道墙 |
| 每次运行上限 | 单次最多归档 120 条 | 洪水被截断 |
| 归档总量上限 | 超过 20000 条自动从头裁掉 | 防仓库膨胀 |
| 屏蔽名单 | `data/blocked.json`，前台和后台都会过滤 | 屏蔽后立刻从蛛网消失 |
| 信封损坏 | 能拆但拆不开 → 隔离 | 篡改信封被拦下 |
| 冷却 | 同一设备写下一条后 6 小时内再写 → 隔离（`rate.cooldownHours`） | 清掉 localStorage 也绕不过 |
| 被读计数 | 单条消息 400 条、单轮 4000 条 | 刷不出漂亮的数字 |
| 被读的七层 | 形状 → 签名身份（HttpOnly + HMAC）→ 工作量证明 → 全站 600/天 → 一个身份 1/天 → 一张网 3/天 → **同一条愿望 1/天** | 清 localStorage 无效；其中四层不看身份，单条愿望一天只涨一格 |
| 远程删除 | 只执行用口令派生的 KEK 密封过的指令；nonce 防重放；只认 `w_…` 形状的 id | 拆不开就丢弃，不报错 |

**隔离 ≠ 删除。** 被拦下的内容进 `data/queue.jsonl`（整条加密），
在后台"待审"区能看到原文和拦截理由，再用一条命令放行或屏蔽：

```bash
WISH_ADMIN_PASSPHRASE=<口令> node scripts/moderate.mjs list
WISH_ADMIN_PASSPHRASE=<口令> node scripts/moderate.mjs approve <id>
node scripts/moderate.mjs reject <id>
```

前端也会用同一份 `data/moderation.json` 先拦一道，用户当场就知道哪里不合适，
不用等采集器把内容吞掉。

---

## 四、后台自身的防护

- 所有用户内容一律 `textContent` 渲染，**从不拼接 innerHTML** → 没有 XSS 面。
- 导出 CSV 会先把 `=`、`+`、`-`、`@`、Tab 开头的单元格加单引号前缀 → 防 Excel 公式注入。
- `admin.html` 带 `noindex, nofollow`。
- 口令错误时不区分"口令不存在"和"口令错误"，也不回显提示。
- **远程删除不需要上网外的任何新凭据。** 放映室用的是解锁之后已经在内存里的 KEK：
  把要删的 id 列表用 AES-256-GCM 封一段，投到同一个中转站，采集器用口令派生出的同一把 KEK 拆开才执行。
  口令不落盘、不上传；中间人看到的是密文，伪造不了（AES-GCM 会先失败）。

---

## 五、这些防护挡不住的（诚实清单）

**已经堵上的：**

- ✅ **私密愿望不会再出现在公开仓库里。** 它进的是 `data/private/vault.jsonl`，全密文；
  公开归档里一条都没有。仓库公开这件事，对私密内容不再构成泄露。
- ✅ **换口令不再丢历史数据。** DEK/KEK 分层之后，`node scripts/set-passphrase.mjs "新" "旧"` 
  只重新包一次密钥环，历史元数据与私密愿望照旧可读。
- ✅ **限流不再只依赖客户端自报字段。** 新增服务端强制的**全站每小时 240 条**配额，
  伪造多少设备号都要撞这道墙。

**仍然存在的（诚实清单）：**

1. **`dv`（设备标识）仍然是客户端自报的**，可以伪造，从而绕过"每设备 6 条/小时"。
   兜底的是工作量证明（每条约 0.45 秒 CPU）+ 全站配额 + 单次 120 条上限。
   要彻底解决必须有服务端观测的身份（IP / Cookie），而中转站不转发 IP。
2. **私密愿望在采集器内存里会短暂以明文出现。** GitHub Actions 必须解开它才能做内容审查，
   这是"要审查"与"完全零知识"之间无法两全的地方。接受不了就只能放弃服务端审查。
3. **ntfy 话题是公开的。** 知道话题名的人可以读公开愿望、也可以投递（私密愿望只有密文可读）。
   话题名是 14 位随机串，换话题的方式写在 README 里。
4. **屏蔽/放行需要一次提交才生效。** 没有服务端就没有实时封禁；
   从执行带 `--push` 的审核命令到 Pages 重建生效大约一分钟。
5. **口令强度决定一切。** 25 万次 PBKDF2 挡得住在线爆破，挡不住离线字典攻击。
   口令丢了，私密愿望和基础信息就永久解不开了——没有找回通道。
   远程删除也依赖它：口令泄露等于内容也能被远程删掉。
6. **远程删除是异步的。** 指令投到中转站后，要等采集器跑一次（约一分钟）才落盘生效；
   采集器没跑，指令就停在缓存里（12 小时后过期）。这也意味着删除不会"立刻封禁"，
   而是"约一分钟后对所有人生效"。
7. **被读计数是七层筛下来的，目标是"接近刷不动"，不是"绝对刷不动"。**
   七层：形状 → 签名身份 → 工作量证明 → 全站当天额度 → 一个身份一天一条 →
   一张出口网一天三条 → 同一条愿望一天只算一次。其中**形状 / 证明 / 全站 / 单条愿望
   四层不看你是谁**，所以伪装成新人也换不来额外收益；而"同一条愿望一天一格"
   意味着"结实"必须由三个不同的日子攒出来，一晚上的刷子推不动任何一条。
   仍然能绕——清掉 cookie 换个身份（还会被出口网挡），再换一个出口地址
   （VPN、手机流量）才是真的从头开始。所以它是"让刷变贵"，不是"刷不动"；
   要更狠只能引入账号或收费，那和这个站的调性不符。
   **我们看了什么**：`/api/read` 会读到来源地址，但只用它算一个当天有效的哈希
   （`HMAC(密钥, 地址 + 当天)`），算完就丢；地址本身不写进任何文件、不进日志、不回传，
   换一天就对不上。写愿望那一步不经过这个函数，也就不看地址。
   **同一张网会挤**：一家人或一个公司共用一个出口地址，所以来源那一层留了 3 条/天；
   真正的闸是 cookie（1 条/天），同一个网络里三个人各自还有一次机会。
   **连这个哈希也不想要**：`WISH_READ_IP_OFF=1` 让函数根本不读来源地址，
   那时唯一化只剩签名 cookie（清 localStorage 无效，清 cookie 仍可重来）。
   **没配 `WISH_READ_SECRET` 时这层关闭**，退回按客户端自报设备号的老办法——
   能跑，但唯一化是弱的；采集器"只认签名"这一点仍然是 fail closed。
8. **「一天只递一次」只记在本机。** 分享的冷却写在 localStorage 里，清掉或换个浏览器就没了；
   而且编号本来就印在揭幕页上，谁也拦不住你念给别人听。它是克制的仪式，不是门锁。
   二维码是本站自己画的（`assets/js/qr.js`，零依赖），链接不会经过任何第三方二维码服务。
6. **GitHub 的定时任务只是"尽力而为"。** `on.schedule` 可能延迟，高峰期甚至会被丢弃，
   而且不保证按点跑。所以前台和后台都**直接读中转站的实时缓存**，归档只负责长期保存；
   另外每条愿望会在**提交者的浏览器里留一份**，下次打开页面时若还没进归档就再投一次
   （采集器按 id 去重）。仍然建议偶尔手动确认一次：`gh workflow run collect-wishes`。

---

## 六、放映室的第二因素：为什么是通行密钥，不是 TOTP

先说结论：**没有做 TOTP（微软/谷歌验证器里那种 6 位码），因为它在这个架构里是表演。**

放映室是纯客户端的：口令不上传，KEK 由 PBKDF2 在浏览器里派生，只存在内存。
TOTP 要在浏览器里验，那 TOTP 的密钥只能放在两个地方之一：

| 放哪 | 结果 |
|---|---|
| 公开仓库 | 谁都能算出验证码，等于没有 |
| 用 KEK 加密后放仓库 | 能用口令解开的东西，挡不住拿到口令的人 |

而拿到口令的人本来就能解开 `data/private/keys.json` 和整个归档。所以那是一道
"看起来安全、实际零成本绕过"的门——比没有门更糟，因为它会让人放松警惕。

**通行密钥（WebAuthn）不一样**：验证只需要**公钥**（公开无所谓），签名私钥留在硬件里
（Windows Hello、手机上的通行密钥、USB 安全钥匙）。拿到口令的人造不出签名。
而且它不需要任何服务端：挑战是自己算的，公钥在仓库里，两边都能独立验。

### 装在两个地方

1. **进放映室**：口令对了之后再弹一次系统验证。这是"别让人顺手翻一遍"。
2. **远程删除**：删除指令必须带上硬件在「要删哪些 id + nonce + 时刻」上的签名，
   **由采集器验**。这一条是硬的——即使有人拿着口令自己拼一条密封指令发到中转站，
   采集器也会因为签名不对而拒收（`删除缺通行密钥` 会出现在采集器日志里）。

### 怎么验的

`tests/passkey-e2e.mjs` 用 Chrome 的**虚拟认证器**（实现的是与 Windows Hello 同一套协议）
把整条链路真跑一遍：注册 → 从 attestationObject 抠出公钥 → 装上 → 口令对了但门不开 →
虚拟认证器签一次 → 进门 → 远程删除时再签一次 → 用户取消签名则门不开 →
跑完把 `passkey.json` 还原成占位（绝不把 `localhost` 的钥匙留在仓库里）。
`tests/webauthn.test.mjs` 另外用软件 P-256 造断言，把十二种伪造/篡改情形逐个打一遍。

### 登记与重置

- **登记**：进放映室 → 右上角「登记通行密钥」→ 下载 `passkey.json` → 覆盖
  `data/private/passkey.json` → 提交推送。
- **必须在域名上登记**：rpId 只能是域名，Chrome 拒绝 IP（`127.0.0.1` 报 invalid domain）。
  本机自测用 `http://localhost:4173`；正式那把钥匙在长期域名上登记。
- **为什么不让页面直接写**：登记必须由有仓库写权限的人完成。如果页面能写，
  拿到口令的人就能给自己登记一把钥匙，第二因素就白装了。
- **重置**：把那个文件改回 `installed: false` 提交推送。
- 换域名（比如从 `*.vercel.app` 换到自定义域名）会换掉 rpId，需要重新登记一把。

---

## 七、渗透测试

两个脚本，随时可以重跑：

```bash
node scripts/serve.mjs 4173        # 另开一个终端
node tests/pentest.mjs             # 前端：输入框与所有渲染路径
node tests/pentest-server.mjs      # 服务端：/api/read 与采集器
```

### 打了什么

前端塞了 **17 组载荷**（`<script>`、`<img onerror>`、`<svg onload>`、`srcdoc` 的 iframe、
`javascript:` 伪协议、`<math><mglyph>` 这条老绕法、`<template>`、`<noscript>`、RTL 覆盖、
NULL 字节、两千字的重复标签……），从**九条路**进去：名字与愿望输入框、提交后的揭晓页、
伪造的分享 token、被改过的 localStorage、蛛网露珠提示、读屏清单、全屏放映、
查编号输入框、回响（云与列）、放映室的表格与抽屉。

| 结论 | 说明 |
|---|---|
| 没有一处执行 | 全站零 `innerHTML`（唯一一处是揭幕页克隆自己的动画结构，不含用户内容），用户内容一律 `textContent` |
| 没有节点被注入 | 载荷进来是字，不是标签 |
| 原型没被污染 | 分享 token / localStorage / 请求体里塞 `__proto__` 都不行 |
| 没有崩溃 | 超长 token、垃圾字段、类型全错，页面照常在 |
| 报错不吐内部信息 | 响应里没有堆栈、没有密钥 |

服务端打的是：形状（`ids` 是字符串 / 对象 / null / 全垃圾）、原型污染、超长 `x-forwarded-for`、
头部里塞 CRLF、cookie 里塞属性、陌生来源、错误方法，以及往采集器里灌
`__proto__` / `constructor` 当 id、没有签名的读、坏掉的删除指令。

### 这一轮修掉的

1. `/api/read` 对「整批都不是愿望编号」的请求返回 200 —— 现在 400 早退。
   （只拒整批，不筛单条：工作量证明是按原样那一串算的。）
2. 采集器把来源代号和身份直接当对象的键 —— 现在代号必须是 24 位十六进制、身份必须是 base64url，
   恶意值进不了 `rl.readIps['__proto__']` 这种位置。
3. 远程删除指令的 id 数组没有上限 —— 现在一次最多 200 条。
4. 分享 token 没有长度上限 —— 现在超过 4096 字符直接当作读不出来。
5. 分享 token 里的「丝」被直接写进 `data-thread` —— 现在过一遍 `threadById` 归一化。

### 补上的响应头

`vercel.json` 与本地 `scripts/serve.mjs` 发同一套：
`Content-Security-Policy`（`default-src 'self'`，脚本只准同源，`object-src 'none'`、
`base-uri 'none'`、`form-action 'none'`、`frame-ancestors 'none'`）、
`X-Content-Type-Options: nosniff`、`Referrer-Policy`、`X-Frame-Options: DENY`、`HSTS`、
`Permissions-Policy`。为了让 `style-src` 不必开 `'unsafe-inline'`，
原来那一处内联样式挪进了 `base.css`。本地跑的就是线上策略。
**换中转站话题时记得同步 `connect-src`。**

### 仍然开着的问题（不装作没有）

（其中两条在这之后已经处理：`/api/poke` 的按 IP 限流见 `lib/kv.js`；
放映室的第二因素见上面第六节。）

- **`/api/poke` 的按 IP 限流要配了 KV 才成立。** 函数是无状态的，同一个实例里的计数器换一个实例就没了，
  所以"每 IP 几次"必须有外部存储。代码已按 Vercel KV / Upstash 的 REST 接口写好
  （`lib/kv.js`，纯 HTTP，不引 npm 包）：配了 `KV_REST_API_URL` + `KV_REST_API_TOKEN`
  （或 `UPSTASH_REDIS_REST_*`）就是真的每 5 分钟 6 次；没配就放行，响应里会标明。
  **KV 连不上时放行**（限流失败不挡正常用户），这是取舍，不是疏漏。
  KV 里存的是「来源地址的当天代号」，不是地址本身。
- **来源代号在当天内是稳定的**，所以那 24 小时里它是一个（很弱的）跟踪信号。
  已披露；`WISH_READ_IP_OFF=1` 可以整层关掉。
- **放映室的第二因素是通行密钥，但它保护的是"动作"，不是"数据"。**
  装了通行密钥之后，进放映室要它签一次，远程删除还要它单独签一次
  （采集器验签，所以自己拼一条密封的删除指令也过不了）——**没有硬件就删不掉**。
  但要说清楚：归档本身的机密性始终只挂在口令上。有口令的人可以绕开界面、用脚本
  直接解开 `data/private/*`。第二因素挡的是"拿着口令就把东西删了/看个遍"，
  挡不住"拿着口令离线解密"。要让数据本身也需要硬件，得用 WebAuthn 的 PRF 扩展把密钥
  也绑在硬件上，那会带来"钥匙丢了数据就永久打不开"的风险，这个站不做。
  **钥匙丢了**：把 `data/private/passkey.json` 改回 `installed: false` 提交推送，回到纯口令模式。
- **`/api/read` 是公开端点**，任何人都能往公共中转站发「被读」消息。它写不了愿望、删不了东西，
  只能试着多加几次读数——而那被七层挡着。

---

## 八、运维清单

- [ ] 后台口令改掉默认值（`scripts/set-passphrase.mjs`）
- [ ] 同步 GitHub Secret：`gh secret set WISH_ADMIN_PASSPHRASE --body "<口令>"`
- [ ] `data/moderation.json` 的违禁词按你的场景增删
- [ ] 定期在后台看"待审"区
- [ ] 出问题时：`reject` 单条，或换掉 ntfy 话题（旧归档不会丢）
