# DESIGN.md · 一线千愿

## 物理场景（决定一切的那句话）

> 凌晨两点，一个人把额头贴在窗上。窗外是一片过曝的、几乎发白的水面。
> 她正在看一部还没拍出来的电影预告片，而她自己写的愿望，正要成为它的下一帧。

**结论：光是浅色的。** 不是深色模式、不是因为工具都长这样才选深色。
参考图（MAD 预告片字卡）本身就是过曝的浅色——纸白、薄荷、珊瑚红。主题跟着它们走。

## 参考定位（aesthetic lane）

不是杂志排版，不是终端风，是**电影预告片字卡**：
高对比度拉长体大字 + 字距很宽的小型大写拉丁副标题 + 星标 + `COMING SOON`。
参考图里的日本明朝体、`蜘蛛糸モノポリー` 的珊瑚红、`bilibili VECTOR` 的青绿，
就是这套视觉的全部来源。

## 颜色策略

**Drenched**（整个表面就是那片光）+ **Full palette**（四条被命名的丝线）。

| 名称 | 值 | 用途 |
|---|---|---|
| `--paper` | `oklch(0.9745 0.0115 92)` | 过曝的暖白纸面 |
| `--water` | `oklch(0.9180 0.0430 187)` | 水面的薄荷 |
| `--ink` | `oklch(0.3350 0.0380 205)` | 墨（深青，不是黑） |
| `--silk` 蜘蛛糸 | `oklch(0.6700 0.1300 32)` | 默认强调色 / 珊瑚 |
| `--abyss` 深海 | `oklch(0.5600 0.0850 197)` | 青 |
| `--moon` 月白 | `oklch(0.7450 0.0900 78)` | 淡金 |
| `--dusk` 暮红 | `oklch(0.5200 0.1650 26)` | 深绯 |

- 全部用 OKLCH。没有任何 `#000` 或 `#fff`——白色一律是带色的纸白。
- 色彩随 `[data-thread]` 切换：换一条丝，整站强调色跟着走。

## 字体

| 角色 | 字体 | 为什么是它 |
|---|---|---|
| 中文大字 | **Noto Serif SC** 900 | 参考图的明朝体就是这个 |
| 拉丁大字 | **Bodoni Moda** | 参考图 `MONOPOLY` / `SKAMIKAZE` 的拉长体 |
| 字卡副标题 | **Cinzel** | `A MAD BY HATSUNE MIKU` 那种字距很宽的小型大写 |
| 界面/数据 | **Jost** | 电影海报的 Futura 传统，库布里克那一路 |

字体装载策略：三个拉丁字体**自托管**（合计约 290KB，`scripts/fetch-fonts.mjs` 生成）；
中文体积太大，运行时按 Google → `fonts.loli.net` → `fonts.geekzu.org` 顺序试，
全不通就退到系统宋体栈（`assets/js/fonts.js`）。
接线在 `assets/js/app.js` 的 `boot()` 与 `admin.js`，都**不 await** —— 字体晚到不该把页面挡在半路，
所以首屏可能先是一版系统宋体、随后换过去。

## 排版尺度

`--t-micro` 0.645 → `--t-hero` clamp(3.6rem, 15.5vw, 12.5rem)，步进约 1.26×。
海报里的字**自适应缩放**（`fitPoster`），一行多少字由测量决定，不由断点决定。

## 布局

不对称。那根丝固定在视口 `--thread-x: 26%`，所有内容相对它错位排布。
海报是 16:9——因为参考图全都是 16:9 的视频帧。
不用卡片。整站没有一张「卡片」，只有线、留白和字。

## 动效

- 只动 `transform` / `opacity` / `font-size`，不动布局属性。
- 缓动一律 `ease-out-expo` / `ease-out-quart`。没有回弹、没有弹性。
- 转场是「换镜」：一层纸白的幕布闪一下（`--dur-2`），不是滑动。
- 尊重 `prefers-reduced-motion`：蛛网只画一帧，颗粒静止。

## 明确避开的东西

无渐变文字、无侧边色条、无玻璃拟态、无 hero 大数字三件套。
后台的概览写成一句话——「今夜，蛛丝上多了 8 个愿望。」——而不是四个大数字。

## 组件

`.poster`（海报）、`.btn`、`.write`（写作输入）、`.thread-pick`（选丝）、
`.chip`、`.web-tip`（露珠提示）、`.drawer`（后台详情抽屉）。
