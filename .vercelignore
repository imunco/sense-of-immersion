# 站点本体已经交给 Vercel 输出（见 build-vercel.mjs），所以这里不再排除页面和资源。
# 只排除上传时不需要的东西。（.gitignore 语义：不带斜杠的模式会匹配任意层级，所以一律用前导 / 锚定）
/.git
/.github
/.vercel
/.dsh-passphrase.local
/node_modules
/backups
/tests
/scripts
/*.md
/package-lock.json
/*.ps1
/.gitignore
/.gitattributes
